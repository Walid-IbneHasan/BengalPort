<script lang="ts">import {onMount} from 'svelte';import {api} from '$lib/api';import DivisionPortal from '$lib/components/DivisionPortal.svelte';import {defaultHealthcareContent,type DivisionContent} from '$lib/division-content';let content:DivisionContent=structuredClone(defaultHealthcareContent),records:any[]=[],loading=true;onMount(async()=>{try{const [page,data]=await Promise.all([api<{content:DivisionContent}>('/content/healthcare'),api<any[]>('/healthcare')]);content=page.content;records=data}catch{}finally{loading=false}})</script>
<svelte:head><title>Global Healthcare — Bengal Port</title><meta name="description" content={content.hero.description}/></svelte:head><DivisionPortal {content} {records} {loading} kind="healthcare"/>
